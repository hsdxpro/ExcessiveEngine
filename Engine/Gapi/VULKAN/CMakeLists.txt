#-------------------------------------------------------------------------------
# GRAPICS API VULKAN
#
# Wrapper for the VULKAN API.
#-------------------------------------------------------------------------------

message( "-GraphicsApiVULKAN" )

# enumerate sources
FILE( GLOB_RECURSE sources *.c* )
FILE( GLOB_RECURSE headers *.h* )